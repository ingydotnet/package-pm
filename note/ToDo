== To Do
- All extra options go into stash
- Any single variable can go in path
- Config can map cmd args to stash vars
- Use config order:
  - pkg -f conf.yaml
  - $PKG_CONF
  - ./pkg/conf.yaml
  - $PKG_ROOT/conf.yaml
  - ~/.pkg/conf.yaml

== Commands
> package new --module=Foo::Bar --from=perl/package
> package new --module=Foo::Bar --from=perl/module-install
> package new --module=Foo::Bar --from=perl/module-build
